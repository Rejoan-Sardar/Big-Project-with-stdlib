  

'use strict';

// MODULES //

var tape = require( 'tape' );
var objectKeys = require( '@stdlib/utils/keys' );
var pareto1 = require( './../lib' );


// TESTS //

tape( 'main export is an object', function test( t ) {
	t.ok( true, __filename );
	t.strictEqual( typeof pareto1, 'object', 'main export is an object' );
	t.end();
});

tape( 'the exported object contains Pareto (Type I) distribution functions', function test( t ) {
	var keys = objectKeys( pareto1 );
	t.equal( keys.length > 0, true, 'has keys' );
	t.end();
});
