  

'use strict';

// MODULES //

var tape = require( 'tape' );
var objectKeys = require( '@stdlib/utils/keys' );
var geometric = require( './../lib' );


// TESTS //

tape( 'main export is an object', function test( t ) {
	t.ok( true, __filename );
	t.strictEqual( typeof geometric, 'object', 'main export is an object' );
	t.end();
});

tape( 'the exported object contains geometric distribution functions', function test( t ) {
	var keys = objectKeys( geometric );
	t.equal( keys.length > 0, true, 'has keys' );
	t.end();
});
