  

'use strict';

// MODULES //

var tape = require( 'tape' );
var objectKeys = require( '@stdlib/utils/keys' );
var poisson = require( './../lib' );


// TESTS //

tape( 'main export is an object', function test( t ) {
	t.ok( true, __filename );
	t.strictEqual( typeof poisson, 'object', 'main export is an object' );
	t.end();
});

tape( 'the exported object contains Poisson distribution functions', function test( t ) {
	var keys = objectKeys( poisson );
	t.equal( keys.length > 0, true, 'has keys' );
	t.end();
});
