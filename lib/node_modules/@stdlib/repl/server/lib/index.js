  

'use strict';

/**
* REPL environment.
*
* @module @stdlib/repl/server
*
* @example
* var repl = require( '@stdlib/repl/server' );
*
* repl( onStart );
*
* function onStart( error, server ) {
*     if ( error ) {
*         throw error;
*     }
*     server.close();
* }
*/

// MODULES //

var main = require( './main.js' );


// EXPORTS //

module.exports = main;
