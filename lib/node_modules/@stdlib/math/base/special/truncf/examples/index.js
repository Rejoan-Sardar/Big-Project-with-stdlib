  'use strict';

var randu = require( '@stdlib/random/base/randu' );
var truncf = require( './../lib' );

var x;
var i;

for ( i = 0; i < 100; i++ ) {
	x = (randu()*100.0) - 50.0;
	console.log( 'trunc(%d) = %d', x, truncf( x ) );
}
