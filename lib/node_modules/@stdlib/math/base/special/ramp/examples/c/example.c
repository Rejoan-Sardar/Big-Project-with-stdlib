  #include "stdlib/math/base/special/ramp.h"
#include <stdio.h>

int main( void ) {
	const double x[] = { 3.14, -3.14, 0.0, 0.0/0.0 };

	double y;
	int i;
	for ( i = 0; i < 4; i++ ) {
		y = stdlib_base_ramp( x[ i ] );
		printf( "R(%lf) = %lf\n", x[ i ], y );
	}
}
