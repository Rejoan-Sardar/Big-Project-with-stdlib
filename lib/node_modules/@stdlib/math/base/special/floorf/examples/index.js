  'use strict';

var randu = require( '@stdlib/random/base/randu' );
var floorf = require( './../lib' );

var x;
var i;

for ( i = 0; i < 100; i++ ) {
	x = (randu()*100.0) - 50.0;
	console.log( 'floorf(%d) = %d', x, floorf( x ) );
}
