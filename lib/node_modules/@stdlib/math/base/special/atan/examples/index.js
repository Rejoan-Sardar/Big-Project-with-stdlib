  

'use strict';

var linspace = require( '@stdlib/array/base/linspace' );
var atan = require( './../lib' );

var x = linspace( -1000.0, 1000.0, 100 );

var i;
for ( i = 0; i < x.length; i++ ) {
	console.log( 'atan(%d) = %d', x[ i ], atan( x[ i ] ) );
}
