  

'use strict';

var linspace = require( '@stdlib/array/base/linspace' );
var fresnelc = require( './../lib' );

var x = linspace( 0.0, 10.0, 100 );

var i;
for ( i = 0; i < x.length; i++ ) {
	console.log( 'fresnelc(%d) = [ %d, %d ]', x[ i ], fresnelc( x[ i ] ) );
}
