  

'use strict';

var randu = require( '@stdlib/random/base/randu' );
var expm1rel = require( './../lib' );

var x;
var y;
var a;
var i;

for ( i = 0; i < 100; i++ ) {
	x = (randu()*100.0) - 50.0;
	a = x.toFixed( 3 );
	y = expm1rel( x );
	console.log( '(e^%d - 1)/%d = %d', a, a, y );
}
