  

'use strict';

var linspace = require( '@stdlib/array/base/linspace' );
var sincospi = require( './../lib' );

var x = linspace( 0.0, 2.0, 101 );

var y;
var i;
for ( i = 0; i < x.length; i++ ) {
	y = sincospi( x[ i ] );
	console.log( 'sincospi(%d) = [ %d, %d ]', x[ i ], y[ 0 ], y[ 1 ] );
}
