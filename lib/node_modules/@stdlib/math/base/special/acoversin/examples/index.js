  

'use strict';

var linspace = require( '@stdlib/array/base/linspace' );
var acoversin = require( './../lib' );

var x = linspace( 0.0, 2.0, 100 );

var i;
for ( i = 0; i < x.length; i++ ) {
	console.log( 'acoversin(%d) = %d', x[ i ], acoversin( x[ i ] ) );
}
