  

'use strict';

var linspace = require( '@stdlib/array/base/linspace' );
var fresnel = require( './../lib' );

var x = linspace( 0.0, 10.0, 100 );

var y;
var i;
for ( i = 0; i < x.length; i++ ) {
	y = fresnel( x[ i ] );
	console.log( 'fresnel(%d) = [ %d, %d ]', x[ i ], y[ 0 ], y[ 1 ] );
}
