  #include "stdlib/math/base/special/invf.h"
#include <stdio.h>

int main( void ) {
	const float x[] = { 3.0f, 4.0f, 5.0f, 12.0f };

	float y;
	int i;
	for ( i = 0; i < 4; i++ ) {
		y = stdlib_base_invf( x[ i ] );
		printf( "invf(%f) = %f\n", x[ i ], y );
	}
}
