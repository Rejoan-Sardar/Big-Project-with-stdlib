  

'use strict';

var linspace = require( '@stdlib/array/base/linspace' );
var acoth = require( './../lib' );

var x = linspace( 1.0, 5.0, 100 );

var i;
for ( i = 0; i < x.length; i++ ) {
	console.log( 'acoth(%d) = %d', x[ i ], acoth( x[ i ] ) );
}
