  #include "stdlib/math/base/special/inv.h"
#include <stdio.h>

int main( void ) {
	const double x[] = { 3.0, 4.0, 5.0, 12.0 };

	double y;
	int i;
	for ( i = 0; i < 4; i++ ) {
		y = stdlib_base_inv( x[ i ] );
		printf( "inv(%lf) = %lf\n", x[ i ], y );
	}
}
