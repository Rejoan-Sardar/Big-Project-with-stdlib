  

'use strict';

var linspace = require( '@stdlib/array/base/linspace' );
var ahaversin = require( './../lib' );

var x = linspace( 0.0, 1.0, 100 );

var i;
for ( i = 0; i < x.length; i++ ) {
	console.log( 'ahaversin(%d) = %d', x[ i ], ahaversin( x[ i ] ) );
}
