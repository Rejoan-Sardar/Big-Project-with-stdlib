  

'use strict';

var linspace = require( '@stdlib/array/base/linspace' );
var acos = require( './../lib' );

var x = linspace( -1.0, 1.0, 100 );

var i;
for ( i = 0; i < x.length; i++ ) {
	console.log( 'acos(%d) = %d', x[ i ], acos( x[ i ] ) );
}
