  #include "stdlib/math/base/special/abs2.h"
#include <stdio.h>

int main( void ) {
	const double x[] = { 3.14, -3.14, 0.0, 0.0/0.0 };

	double y;
	int i;
	for ( i = 0; i < 4; i++ ) {
		y = stdlib_base_abs2( x[ i ] );
		printf( "abs2(%lf) = %lf\n", x[ i ], y );
	}
}
