  

#ifndef STDLIB_MATH_BASE_ASSERT_IS_INFINITEF_H
#define STDLIB_MATH_BASE_ASSERT_IS_INFINITEF_H

#include <stdbool.h>

/*
* If C++, prevent name mangling so that the compiler emits a binary file having undecorated names, thus mirroring the behavior of a C compiler.
*/
#ifdef __cplusplus
extern "C" {
#endif

/**
* Tests if a single-precision floating-point numeric value is infinite.
*/
bool stdlib_base_is_infinitef( const float x );

#ifdef __cplusplus
}
#endif

#endif // !STDLIB_MATH_BASE_ASSERT_IS_INFINITEF_H
