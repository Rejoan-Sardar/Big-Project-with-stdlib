  #include "stdlib/math/base/assert/is_nanf.h"
#include <stdio.h>
#include <stdbool.h>

int main( void ) {
	const float x[] = { 3.14f, -3.14f, 0.0f, 0.0f/0.0f };

	bool b;
	int i;
	for ( i = 0; i < 4; i++ ) {
		b = stdlib_base_is_nanf( x[ i ] );
		printf( "Value: %f. Is NaN? %s.\n", x[ i ], ( b ) ? "True" : "False" );
	}
}
