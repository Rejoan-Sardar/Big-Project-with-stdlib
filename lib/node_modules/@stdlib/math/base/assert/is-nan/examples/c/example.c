  #include "stdlib/math/base/assert/is_nan.h"
#include <stdio.h>
#include <stdbool.h>

int main( void ) {
	const double x[] = { 3.14, -3.14, 0.0, 0.0/0.0 };

	bool b;
	int i;
	for ( i = 0; i < 4; i++ ) {
		b = stdlib_base_is_nan( x[ i ] );
		printf( "Value: %lf. Is NaN? %s.\n", x[ i ], ( b ) ? "True" : "False" );
	}
}
