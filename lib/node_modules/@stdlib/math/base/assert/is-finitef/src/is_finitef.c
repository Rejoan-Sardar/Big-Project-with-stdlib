  #include "stdlib/math/base/assert/is_finitef.h"
#include <stdbool.h>
#include <math.h>

/**
* Tests if a single-precision floating-point number is finite.
*
* @param x       number
* @return        boolean indicating if an input value is finite
*/
bool stdlib_base_is_finitef( const float x ) {
	return ( x == x && x < INFINITY && x > -INFINITY );
}
