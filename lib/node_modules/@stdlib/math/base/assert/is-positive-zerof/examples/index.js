  'use strict';

var isPositiveZerof = require( './../lib' );

console.log( isPositiveZerof( 0.0 ) );
// => true

console.log( isPositiveZerof( -0.0 ) );
// => false

console.log( isPositiveZerof( 5.0 ) );
// => false

console.log( isPositiveZerof( -1.0 ) );
// => false

console.log( isPositiveZerof( NaN ) );
// => false
