  #include "stdlib/math/base/assert/is_positive_zero.h"
#include <stdio.h>
#include <stdbool.h>

int main( void ) {
	const double x[] = { 0.0, -0.0, 3.14, 0.0/0.0 };

	bool b;
	int i;
	for ( i = 0; i < 4; i++ ) {
		b = stdlib_base_is_positive_zero( x[ i ] );
		printf( "Value: %lf. Is +0? %s.\n", x[ i ], ( b ) ? "True" : "False" );
	}
}
