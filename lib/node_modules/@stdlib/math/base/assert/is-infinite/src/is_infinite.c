  

#include "stdlib/math/base/assert/is_infinite.h"
#include <stdbool.h>
#include <math.h>

/**
* Tests if a double-precision floating-point number is infinite.
*
* @param x       number
* @return        boolean indicating if an input value is infinite
*/
bool stdlib_base_is_infinite( const double x ) {
	return ( x == HUGE_VAL || x == -HUGE_VAL );
}
